#ifndef FOODITEM_H\n#define FOODITEM_H\n\n#include <string>\n\nclass FoodItem {\n    private: \n        std::string name;\n        double calories;\n        double price;\n\n    public: \n        FoodItem(const std::string& name, double calories, double price) : name(name), calories(calories), price(price) {}\n        std::string getName() const { return name; }\n        double getCalories() const { return calories; }\n        double getPrice() const { return price; }\n};\n\n#endif // FOODITEM_H